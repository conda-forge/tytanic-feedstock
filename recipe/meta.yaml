{% set name = "tytanic" %}
{% set version = "0.3.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/tingerrr/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: a78bdbcc337d28b85d6b0ba7ac94e2d300cd4c6ebfea59a474d24f6081376b56

build:
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - {{ compiler('rust') }}
    - cargo-bundle-licenses
  host:
    - openssl  # [linux]
  run:
    - openssl  # [linux]

test:
  commands:
    - tt --version

about:
  home: https://github.com/tingerrr/tytanic
  summary: 'A visual regression test runner for typst packages.'
  license: MIT
  license_file:
    - LICENSE
    - THIRDPARTY_LICENSES.yaml

extra:
  recipe-maintainers:
    - freundTech
