{% set name = "tytanic" %}
{% set version = "0.3.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/tingerrr/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: 81077d3157681aba99816ebb54732ca005bbc7de151e8a1cce09281cfed5497e

build:
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - {{ compiler('rust') }}
    - cargo-bundle-licenses
  host:
    - openssl  # [linux]
  run:
    - openssl  # [linux]

test:
  commands:
    - tt --version

about:
  home: https://github.com/tingerrr/tytanic
  summary: 'A visual regression test runner for typst packages.'
  license: MIT
  license_file:
    - LICENSE
    - THIRDPARTY_LICENSES.yaml

extra:
  recipe-maintainers:
    - freundTech
